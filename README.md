# datahelper-site
A lightweight Chrome extension (Manifest V3) that adds context menu actions to clean messy data before copying it to your clipboard. Supports phone numbers, URLs (strips tracking parameters and fragments), and formatted numbers (plain or decimal). Zero dependencies.
